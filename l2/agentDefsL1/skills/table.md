<!-- mls fileReference="_102021_/l2/agentDefsL1/skills/table.md" enhancement="_blank" -->

# table

Logical `tableId` plus the physical name taken from the ontology storage
table. The inventory reads `data.tableId` and uses `artifactId` as the entity,
so `data.entityId` must equal `artifactId`. The adapter cites `tableId`, not a
second physical name.
